{
  "name": "aegiscloud-backend",
  "version": "2.0.0",
  "description": "AegisCloud REST API — Invitations, Org Verification, Email OTP (Resend + Cloudflare Email Routing)",
  "main": "server.js",
  "scripts": {
    "start": "node -r dotenv/config server.js",
    "dev": "nodemon -r dotenv/config server.js"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch-logs": "^3.1091.0",
    "@aws-sdk/client-dynamodb": "^3.1091.0",
    "@aws-sdk/client-s3": "^3.1091.0",
    "@aws-sdk/lib-dynamodb": "^3.1091.0",
    "@aws-sdk/s3-request-presigner": "^3.1091.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
